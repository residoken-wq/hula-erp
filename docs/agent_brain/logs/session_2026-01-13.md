# Session Log - 13/01/2026

## Timeline

### 13:00 - ProfilePage Bugs
- Fixed `formatMoney` function - values from API are strings, need `Number()` conversion
- Fixed deduction total calculation bug - was concatenating strings

### 13:27 - Leave Tab Enhancement
- Enhanced leave balance display with 3 main stats
- Added fallback calculation when `/hr/balance` API fails

### 15:19 - Database Check
- Confirmed foreign key constraint errors are harmless (constraints already exist)

### 15:25 - NPM Proxy Configuration
- Explained changing IP `136.111.215.241` to container name `hula_app`
- Containers need to join same docker network `proxy_net`

### 15:34 - Upload File Serving Issue
- Investigated `net::ERR_BLOCKED_BY_ORB` error
- Files exist in container but response body empty
- Fixed by switching from `res.sendFile()` to `fs.createReadStream().pipe()`
- Added CORS headers

### 15:43 - Image Compression
- Installed `sharp` library
- Implemented auto-compression on upload
- Max 1920px, JPEG 80% quality

### 15:52 - Docker Config Verification
- Confirmed `hula_app` uses `DB_HOST=hula_db` (container name, not IP)
- `hula_frontend` doesn't connect to DB directly

### 15:55 - Final ORB Fix
- Added `Access-Control-Allow-Origin: *` header
- Added `X-Content-Type-Options: nosniff` header

## Files Modified
1. `frontend/src/pages/ProfilePage.tsx`
2. `src/upload/upload.service.ts`
3. `package.json` (added sharp)
